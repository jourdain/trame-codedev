trame:
  cmd:
    - python
    - -m
    - trame.app.demo
    - --host
    - ${host}
    - --port
    - ${port}
    - --authKey
    - ${secret}
    - --server
pan3d:
  app: xr-viewer
  args:
    - --wasm
